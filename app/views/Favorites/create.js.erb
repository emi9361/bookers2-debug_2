if (locatio.pathname == '/books'){
//投稿一覧
  $('.book_<%= @book.id %>').html("<%= j(render partial: 'books/book',  book: @book) %>");
} else {
//投稿詳細
  $('.book_<%= @book.id %>').html("<%= j(render 'books/detail',  book: @book) %>");
}